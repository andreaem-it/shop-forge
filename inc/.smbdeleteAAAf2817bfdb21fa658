<?php
/**
 * BnCom — Tracker visivo stato ordine
 *
 * Mostra un indicatore a step nella pagina dettaglio ordine
 * (My Account → Visualizza ordine).
 * Step: Ricevuto → Pagato → In Preparazione → Spedito → Consegnato
 *
 * @package BnComShopAccount
 */

defined( 'ABSPATH' ) || exit;


// -------------------------------------------------------------------------
// Mappa: stato WooCommerce → livello di avanzamento (1-6)
// 6 = tutti gli step completati (ordine consegnato)
// -------------------------------------------------------------------------

function bncom_order_tracker_progress( string $status ): int {
	$map = [
		'pending'    => 1, // Ricevuto (in attesa pagamento)
		'on-hold'    => 1, // Ricevuto (in attesa verifica)
		'processing' => 3, // Ricevuto ✓ · Pagato ✓ · In Preparazione ←
		'spedito'    => 4, // + Spedito ←
		'completed'  => 6, // tutti ✓ (progress > 5 → tutti is-completed)
	];
	return $map[ $status ] ?? 1;
}


// -------------------------------------------------------------------------
// Render del tracker
// -------------------------------------------------------------------------

function bncom_render_order_tracker( WC_Order $order ): void {
	$status   = $order->get_status();
	$progress = bncom_order_tracker_progress( $status );

	$error_statuses = [ 'cancelled', 'failed', 'refunded' ];
	if ( in_array( $status, $error_statuses, true ) ) {
		$label = wc_get_order_status_name( $status );
		?>
		<div class="bncom-order-tracker bncom-order-tracker--error">
			<i class="fa-solid fa-circle-xmark" aria-hidden="true"></i>
			<span>Ordine <strong><?php echo esc_html( $label ); ?></strong> — nessun avanzamento disponibile.</span>
		</div>
		<?php
		return;
	}

	$steps = [
		[ 'label' => 'Ricevuto',        'icon' => 'fa-solid fa-inbox' ],
		[ 'label' => 'Pagato',           'icon' => 'fa-solid fa-credit-card' ],
		[ 'label' => 'In Preparazione',  'icon' => 'fa-solid fa-box-open' ],
		[ 'label' => 'Spedito',          'icon' => 'fa-solid fa-truck' ],
		[ 'label' => 'Consegnato',       'icon' => 'fa-solid fa-circle-check' ],
	];
	?>
	<div class="bncom-order-tracker" role="list" aria-label="Stato ordine">
		<p class="bncom-tracker-header">
			<i class="fa-solid fa-route" aria-hidden="true"></i>
			Stato ordine
		</p>

		<div class="bncom-tracker-steps">
			<?php foreach ( $steps as $i => $step ) :
				$step_num = $i + 1;

				if ( $step_num < $progress ) {
					$css_class = 'is-completed';
					$aria      = 'Completato';
				} elseif ( $step_num === $progress ) {
					$css_class = 'is-active';
					$aria      = 'In corso';
				} else {
					$css_class = 'is-pending';
					$aria      = 'In attesa';
				}
			?>
				<div class="bncom-tracker-step <?php echo esc_attr( $css_class ); ?>"
				     role="listitem"
				     aria-label="<?php echo esc_attr( $step['label'] . ': ' . $aria ); ?>">

					<div class="bncom-tracker-step__bubble">
						<?php if ( 'is-completed' === $css_class ) : ?>
							<i class="fa-solid fa-check" aria-hidden="true"></i>
						<?php else : ?>
							<i class="<?php echo esc_attr( $step['icon'] ); ?>" aria-hidden="true"></i>
						<?php endif; ?>
					</div>

					<span class="bncom-tracker-step__label"><?php echo esc_html( $step['label'] ); ?></span>
				</div>
			<?php endforeach; ?>
		</div>
	</div>
	<?php
}


// -------------------------------------------------------------------------
// Hook: inietta il tracker prima della tabella ordine
// (escludi thank-you page)
// -------------------------------------------------------------------------

add_action( 'woocommerce_order_details_before_order_table', function ( WC_Order $order ) {
	if ( is_wc_endpoint_url( 'order-received' ) ) {
		return;
	}
	bncom_render_order_tracker( $order );
}, 5 );


// -------------------------------------------------------------------------
// CSS account pages (tracker + stili uniformi dettaglio ordine)
// -------------------------------------------------------------------------

add_action( 'wp_head', function () {
	if ( ! is_account_page() ) {
		return;
	}
	?>
	<style id="bncom-order-detail-css">
	/* ============================================================
	   BNCOM — Account: stili uniformi per tutte le sezioni
	   Usa le variabili CSS del plugin bncom-shop-account
	   ============================================================ */

	/* --- Heading sezioni content --- */
	.woocommerce-account .woocommerce-MyAccount-content h2 {
		font-size: 17px;
		font-weight: 800;
		color: var(--bncom-text-main);
		margin: 0 0 18px;
		padding-bottom: 14px;
		border-bottom: 1px solid var(--bncom-border-soft, #EEF2F7);
		line-height: 1.3;
	}

	.woocommerce-account .woocommerce-MyAccount-content h3 {
		font-size: 14px;
		font-weight: 700;
		color: var(--bncom-text-muted);
		margin: 0 0 12px;
	}

	/* --- Barra info ordine (paragrafo con numero / data / stato) --- */
	.woocommerce-account .woocommerce-MyAccount-content > p:first-of-type {
		display: flex;
		flex-wrap: wrap;
		align-items: center;
		gap: 6px 10px;
		padding: 14px 18px;
		margin: 0 0 24px;
		background: var(--bncom-bg-soft);
		border: 1px solid var(--bncom-border);
		border-radius: var(--bncom-radius);
		font-size: 14px;
		color: var(--bncom-text-muted);
		line-height: 1.5;
	}

	.woocommerce-account .woocommerce-MyAccount-content > p:first-of-type mark.order-number,
	.woocommerce-account .woocommerce-MyAccount-content > p:first-of-type mark.order-date {
		background: none;
		color: var(--bncom-text-main);
		font-weight: 700;
	}

	/* Badge stato inline */
	.woocommerce-account mark.order-status {
		display: inline-flex;
		align-items: center;
		padding: 2px 10px;
		border-radius: 999px;
		font-size: 12px;
		font-weight: 700;
		background: #EFF6FF;
		color: #1D4ED8;
	}

	.woocommerce-account mark.order-status.status-completed   { background: #DCFCE7; color: #15803D; }
	.woocommerce-account mark.order-status.status-processing  { background: #DBEAFE; color: #1D4ED8; }
	.woocommerce-account mark.order-status.status-on-hold     { background: #FEF3C7; color: #B45309; }
	.woocommerce-account mark.order-status.status-pending     { background: #F3F4F6; color: #4B5563; }
	.woocommerce-account mark.order-status.status-cancelled,
	.woocommerce-account mark.order-status.status-failed,
	.woocommerce-account mark.order-status.status-refunded    { background: #FEE2E2; color: #B91C1C; }
	.woocommerce-account mark.order-status.status-spedito     { background: #dbeeff; color: #1565C0; }

	/* --- Sezioni dettaglio ordine --- */
	.woocommerce-account .woocommerce-order-details,
	.woocommerce-account .woocommerce-customer-details {
		margin-bottom: 24px;
	}

	/* Tabella prodotti */
	.woocommerce-account table.woocommerce-table--order-details {
		width: 100%;
		border-collapse: collapse;
		background: #fff;
		border: 1px solid var(--bncom-border);
		border-radius: var(--bncom-radius);
		overflow: hidden;
		box-shadow: var(--bncom-shadow);
		font-size: 14px;
	}

	.woocommerce-account table.woocommerce-table--order-details thead th {
		padding: 12px 18px;
		font-size: 11px;
		font-weight: 800;
		text-transform: uppercase;
		letter-spacing: .06em;
		color: var(--bncom-text-muted);
		background: var(--bncom-bg-soft);
		border-bottom: 1px solid var(--bncom-border);
		text-align: left;
	}

	.woocommerce-account table.woocommerce-table--order-details thead th:last-child { text-align: right; }

	.woocommerce-account table.woocommerce-table--order-details tbody td {
		padding: 14px 18px;
		border-bottom: 1px solid var(--bncom-border-soft);
		color: var(--bncom-text-main);
		vertical-align: middle;
	}

	.woocommerce-account table.woocommerce-table--order-details tbody td:last-child {
		text-align: right;
		font-weight: 600;
	}

	.woocommerce-account table.woocommerce-table--order-details tbody tr:last-child td { border-bottom: none; }

	.woocommerce-account table.woocommerce-table--order-details .woocommerce-table__product-name a {
		color: var(--bncom-primary);
		font-weight: 600;
		text-decoration: none;
	}

	.woocommerce-account table.woocommerce-table--order-details .woocommerce-table__product-name a:hover {
		color: var(--bncom-primary-hover);
		text-decoration: underline;
	}

	.woocommerce-account table.woocommerce-table--order-details tfoot { background: var(--bncom-bg-soft); }

	.woocommerce-account table.woocommerce-table--order-details tfoot tr th,
	.woocommerce-account table.woocommerce-table--order-details tfoot tr td {
		padding: 11px 18px;
		font-size: 14px;
		color: var(--bncom-text-muted);
		border-top: 1px solid var(--bncom-border);
	}

	.woocommerce-account table.woocommerce-table--order-details tfoot tr th { text-align: left; }
	.woocommerce-account table.woocommerce-table--order-details tfoot tr td { text-align: right; }

	.woocommerce-account table.woocommerce-table--order-details tfoot tr:last-child th,
	.woocommerce-account table.woocommerce-table--order-details tfoot tr:last-child td {
		color: var(--bncom-text-main);
		font-weight: 800;
		font-size: 15px;
	}

	/* --- Indirizzi cliente --- */
	.woocommerce-account .woocommerce-customer-details {
		display: grid;
		grid-template-columns: 1fr 1fr;
		gap: 20px;
	}

	.woocommerce-account .woocommerce-customer-details section {
		padding: 22px 24px;
		background: #fff;
		border: 1px solid var(--bncom-border);
		border-radius: var(--bncom-radius);
		box-shadow: var(--bncom-shadow);
		font-size: 14px;
		line-height: 1.75;
		color: var(--bncom-text-muted);
	}

	.woocommerce-account .woocommerce-customer-details section h2 {
		font-size: 12px;
		font-weight: 800;
		text-transform: uppercase;
		letter-spacing: .07em;
		color: var(--bncom-text-muted);
		margin: 0 0 14px;
		padding-bottom: 10px;
		border-bottom: 1px solid var(--bncom-border-soft);
	}

	.woocommerce-account .woocommerce-customer-details address {
		font-style: normal;
		color: var(--bncom-text-main);
	}

	/* --- Lista ordini (/ordini/) --- */
	.woocommerce-account table.woocommerce-orders-table {
		border: 1px solid var(--bncom-border);
		border-radius: var(--bncom-radius);
		overflow: hidden;
		box-shadow: var(--bncom-shadow);
		background: #fff;
		font-size: 14px;
		width: 100%;
		border-collapse: collapse;
	}

	.woocommerce-account table.woocommerce-orders-table thead th {
		padding: 12px 16px;
		font-size: 11px;
		font-weight: 800;
		text-transform: uppercase;
		letter-spacing: .06em;
		color: var(--bncom-text-muted);
		background: var(--bncom-bg-soft);
		border-bottom: 1px solid var(--bncom-border);
		text-align: left;
	}

	.woocommerce-account table.woocommerce-orders-table tbody td {
		padding: 13px 16px;
		border-bottom: 1px solid var(--bncom-border-soft);
		color: var(--bncom-text-main);
		vertical-align: middle;
	}

	.woocommerce-account table.woocommerce-orders-table tbody tr:last-child td { border-bottom: none; }
	.woocommerce-account table.woocommerce-orders-table tbody tr:hover td { background: #FAFBFC; }

	.woocommerce-account table.woocommerce-orders-table .woocommerce-orders-table__cell-order-number a {
		color: var(--bncom-primary);
		font-weight: 700;
		text-decoration: none;
	}

	.woocommerce-account table.woocommerce-orders-table mark.order-status {
		display: inline-flex;
		align-items: center;
		padding: 3px 11px;
		border-radius: 999px;
		font-size: 12px;
		font-weight: 700;
		background: var(--bncom-bg-soft);
		color: var(--bncom-text-muted);
	}

	/* --- Form (dettagli account, indirizzi) --- */
	.woocommerce-account .woocommerce-MyAccount-content fieldset {
		border: 1px solid var(--bncom-border);
		border-radius: var(--bncom-radius);
		padding: 22px 24px;
		margin: 0 0 24px;
	}

	.woocommerce-account .woocommerce-MyAccount-content fieldset legend {
		font-size: 13px;
		font-weight: 700;
		color: var(--bncom-text-muted);
		padding: 0 8px;
	}

	.woocommerce-account .woocommerce-MyAccount-content .woocommerce-form-row label {
		font-size: 13px;
		font-weight: 700;
		color: var(--bncom-text-main);
		margin-bottom: 6px;
	}

	.woocommerce-account .woocommerce-MyAccount-content .woocommerce-form-row { margin-bottom: 18px; }

	/* --- Tracker --- */
	.bncom-order-tracker {
		margin-bottom: 24px;
		padding: 24px 28px;
		background: #fff;
		border: 1px solid var(--bncom-border);
		border-radius: var(--bncom-radius);
		box-shadow: var(--bncom-shadow);
	}

	.bncom-tracker-header {
		display: flex;
		align-items: center;
		gap: 8px;
		margin: 0 0 22px;
		font-size: 12px;
		font-weight: 800;
		text-transform: uppercase;
		letter-spacing: .07em;
		color: var(--bncom-text-muted);
	}

	.bncom-tracker-steps { display: flex; align-items: flex-start; position: relative; }

	.bncom-tracker-step {
		display: flex;
		flex-direction: column;
		align-items: center;
		flex: 1;
		position: relative;
		min-width: 0;
	}

	.bncom-tracker-step::before {
		content: "";
		position: absolute;
		top: 21px;
		left: calc(-50% + 22px);
		right: calc(50% + 22px);
		height: 2px;
		background: var(--bncom-border);
		z-index: 0;
		transition: background .35s ease;
	}

	.bncom-tracker-step:first-child::before { display: none; }

	.bncom-tracker-step.is-completed::before,
	.bncom-tracker-step.is-active::before { background: var(--bncom-success); }

	.bncom-tracker-step__bubble {
		position: relative;
		z-index: 1;
		width: 44px;
		height: 44px;
		border-radius: 50%;
		display: flex;
		align-items: center;
		justify-content: center;
		font-size: 17px;
		background: var(--bncom-bg-soft);
		color: var(--bncom-border);
		border: 2px solid var(--bncom-border);
		transition: background .3s, color .3s, border-color .3s, box-shadow .3s;
	}

	.bncom-tracker-step.is-completed .bncom-tracker-step__bubble {
		background: var(--bncom-success); color: #fff; border-color: var(--bncom-success);
	}

	.bncom-tracker-step.is-active .bncom-tracker-step__bubble {
		background: var(--bncom-primary); color: #fff; border-color: var(--bncom-primary);
		box-shadow: 0 0 0 5px rgba(0,111,239,.14);
	}

	.bncom-tracker-step__label {
		margin-top: 10px;
		font-size: 11px;
		font-weight: 700;
		text-align: center;
		line-height: 1.3;
		color: var(--bncom-border);
		transition: color .3s;
		padding: 0 4px;
	}

	.bncom-tracker-step.is-completed .bncom-tracker-step__label { color: var(--bncom-success); }
	.bncom-tracker-step.is-active .bncom-tracker-step__label { color: var(--bncom-primary); }

	.bncom-order-tracker--error {
		display: flex;
		align-items: center;
		gap: 12px;
		color: var(--bncom-danger);
		background: #FEF2F2;
		border-color: #FECACA;
		font-size: 14px;
		font-weight: 600;
	}

	.bncom-order-tracker--error i { font-size: 22px; flex-shrink: 0; }

	/* --- Responsive --- */
	@media (max-width: 860px) {
		.woocommerce-account .woocommerce-customer-details { grid-template-columns: 1fr; }
	}

	@media (max-width: 600px) {
		.bncom-order-tracker { padding: 18px 14px; }

		.bncom-tracker-step__bubble { width: 36px; height: 36px; font-size: 14px; }

		.bncom-tracker-step::before {
			top: 17px;
			left: calc(-50% + 18px);
			right: calc(50% + 18px);
		}

		.bncom-tracker-step__label { font-size: 9px; margin-top: 7px; }

		.woocommerce-account table.woocommerce-table--order-details thead th,
		.woocommerce-account table.woocommerce-table--order-details tbody td,
		.woocommerce-account table.woocommerce-table--order-details tfoot tr th,
		.woocommerce-account table.woocommerce-table--order-details tfoot tr td {
			padding: 10px 12px;
			font-size: 13px;
		}
	}
	</style>
	<?php
} );
